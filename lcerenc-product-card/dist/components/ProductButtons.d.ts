import React from "react";
export interface Props {
    className?: string;
    style?: React.CSSProperties;
}
declare const ProductButtons: ({ className, style }: Props) => React.JSX.Element;
export default ProductButtons;
